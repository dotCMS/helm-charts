{{- if .Values.networkPolicy.allowPrometheusMonitoring.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-prometheus-monitoring
  namespace: {{ .Values.customerName }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.app }}
      cust: {{ .Values.customerName }}
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          {{- range $key, $value := .Values.networkPolicy.allowPrometheusMonitoring.namespaceSelector }}
          {{ $key }}: {{ $value | quote }}
          {{- end }}
    {{- if .Values.networkPolicy.allowPrometheusMonitoring.podSelector }}
    - podSelector:
        matchLabels:
          {{- range $key, $value := .Values.networkPolicy.allowPrometheusMonitoring.podSelector }}
          {{ $key }}: {{ $value | quote }}
          {{- end }}
    {{- end }}
    ports:
    - protocol: TCP
      port: {{ .Values.management.port }}
{{- end }}